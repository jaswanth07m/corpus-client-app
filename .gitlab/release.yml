# Release variables — bump these on develop, then open an MR to main.
# Merging to main with these changed automatically triggers the release pipeline.
variables:
  VERSION_CODE: '141'
  RELEASE_TAG: '1.3.7'
