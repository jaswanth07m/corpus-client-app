# Release variables — bump these when cutting a new release.
# Override VERSION_CODE and RELEASE_TAG via GitLab web UI when triggering a release pipeline.
variables:
  VERSION_CODE: '139'
  RELEASE_TAG: '1.3.5'
  TARGET_BRANCH: 'develop'
